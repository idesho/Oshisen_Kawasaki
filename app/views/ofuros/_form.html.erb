<%= form_with(model: ofuro) do |form| %>
  <% if ofuro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ofuro.errors.count, "error") %> prohibited this ofuro from being saved:</h2>

      <ul>
        <% ofuro.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
